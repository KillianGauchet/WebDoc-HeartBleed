# Workspace Overview

This is currently an empty workspace. No project-specific structure, files, or configurations have been detected.

## Instructions for Gemini

As this workspace is currently empty, please follow these general guidelines:

- **Project Scaffolding:** If asked to start a new project, suggest an appropriate technology stack (e.g., React/Vite for web, FastAPI for Python APIs) based on the user's requirements.
- **Convention Discovery:** As files are added, identify and adhere to the emerging coding styles, directory structures, and naming conventions.
- **Dependency Management:** Always check for and update relevant dependency files (`package.json`, `requirements.txt`, etc.) when adding new libraries or features.

## Getting Started (TODO)

Once a project type is established, update this section with commands for:
- Initializing the environment (e.g., `npm install`, `python -m venv venv`)
- Running the application (e.g., `npm run dev`, `uvicorn main:app`)
- Running tests (e.g., `npm test`, `pytest`)
